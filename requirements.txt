# Compliance Advisory & Triage Agent — pinned dependencies
# ALL versions are pinned to the exact tested set.
# Do NOT run `pip install --upgrade` without re-testing and re-running:
#   python -m src.ingest   (always required after a chromadb version change)
#
# To install:
#   pip install -r requirements.txt

# HTTP requests
requests==2.32.3

# Vector database — PINNED: changing this version requires deleting
# data/chroma_db/ and re-running `python -m src.ingest`
chromadb==0.5.3

# Local embeddings
sentence-transformers==3.0.1

# LangGraph orchestration
langgraph==0.1.19
langchain-core==0.2.23

# Document processing
langchain-community==0.2.10
langchain-text-splitters==0.2.2

# Streamlit UI
streamlit==1.59.2

# Data processing
pandas==2.2.2
numpy==1.26.4

# Environment variables
python-dotenv==1.0.1

# Data validation
pydantic==2.8.2
